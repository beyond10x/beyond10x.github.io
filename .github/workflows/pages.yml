# Generated by `atlas docs reconcile`; edit Atlas delivery intent, not this file.
name: Organization documentation

on:
  workflow_dispatch:
    inputs:
      published_sha:
        description: Exact generated artifact commit on the published branch
        required: true
        type: string
      control_sha:
        description: Exact bot-authored main commit running this caller
        required: true
        type: string

permissions: {}

concurrency:
  group: organization-pages
  cancel-in-progress: false

jobs:
  deploy:
    if: >-
      github.actor == 'b10x-bot[bot]'
      && github.triggering_actor == 'b10x-bot[bot]'
      && github.repository == 'beyond10x/beyond10x.github.io'
      && github.ref == 'refs/heads/main'
      && github.sha == inputs.control_sha
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: beyond10x/website/.github/workflows/deploy-root.yml@7b366ddc41720a6d04fc14d06a8abc713f33f7cf
    with:
      published_sha: ${{ inputs.published_sha }}
      control_sha: ${{ inputs.control_sha }}
